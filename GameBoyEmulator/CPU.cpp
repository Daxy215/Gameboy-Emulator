#include "CPU.h"

void CPU::reset() {
    // Initialize registers and memory
    /*A = F = B = C = D = E = H = L = 0;
    SP = 0xFFFE;
    PC = 0x0100;
    
    // Clear memory
    std::fill(std::begin(memory), std::end(memory), 0);*/
}
